# Server Configuration
PORT=3100

# Storage Configuration
STORAGE_TYPE=filesystem
STORAGE_PATH=.ph/file-storage
DB_PATH=.ph/read-model.db

# Agent Configuration
AGENT_NAME=powerhouse-agent
ENABLE_AUTO_EDIT=true
ENABLE_VALIDATION=true
LOG_LEVEL=info

# Remote Drive Configuration (optional)
# REMOTE_DRIVE_URL=http://localhost:4001/d/preview-10e97b52

# Powerhouse Project Configuration (optional)
# Name of the Powerhouse project to auto-start when server starts
# POWERHOUSE_PROJECT=my-project

# Directory containing all Powerhouse projects (default: ../projects)
# POWERHOUSE_PROJECTS_DIR=../projects

# Optional port overrides for the auto-started project
# Note: Avoid ports like 6000 which are blocked by browsers (unsafe ports)
# Safe ports: 3000-5999, 6100-6999, 7000-9999 (avoiding common services)
# POWERHOUSE_CONNECT_PORT=5000
# POWERHOUSE_SWITCHBOARD_PORT=6100

# Timeout for waiting for project startup in milliseconds (default: 60000)
# POWERHOUSE_STARTUP_TIMEOUT=60000